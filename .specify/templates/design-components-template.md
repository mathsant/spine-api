# Componentes Reutilizáveis

**Última atualização**: [DATA]

<!--
Inventário dos componentes que aparecem repetidos no design (em 2+ telas) ou que o
design marca explicitamente como reutilizáveis. Gerado por /design-import.
O conteúdo gerado é sobrescrito no re-run; o bloco SDD:MANUAL é preservado.

Um bloco `## [Nome]` por componente, seguindo o modelo abaixo.
-->

## [NomeDoComponente]

**Descrição**: [o que é e para que serve, em uma frase.]

**Onde aparece**: [ex.: [Login](screens/login.md), [Cadastro](screens/cadastro.md)]

**Variantes**: [ex.: `primária`, `secundária`, `fantasma`, `perigo`]

**Props / parâmetros**:

| Prop | Tipo | Valores | Padrão | Descrição |
|---|---|---|---|---|
| [ex.: `variante`] | enum | `primária` \| `secundária` | `primária` | [estilo visual] |
| [ex.: `tamanho`] | enum | `sm` \| `md` \| `lg` | `md` | [altura e padding] |
| [ex.: `desabilitado`] | boolean | — | `false` | [estado inativo] |

**Estados**: [ex.: default, hover, focus, active, disabled, loading]

**Tokens usados**: [ex.: `--color-primary`, `--radius-md`, espaçamento 12/16]

**Observações de implementação**: [ex.: precisa de `aria-disabled`; ícone opcional à
esquerda do texto; largura total no mobile.]

---

<!-- SDD:MANUAL:INICIO -->
<!-- Anotações manuais preservadas entre re-execuções de /design-import.
     Ex.: componentes que já existem no código e não precisam ser recriados. -->
<!-- SDD:MANUAL:FIM -->
